{% extends 'base.html.twig' %}

{% block title %}Edit LibraryCard{% endblock %}

{% block body %}
    <h1>Edit LibraryCard</h1>

    {{ include('library_card/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('library_card_index') }}">back to list</a>

    {{ include('library_card/_delete_form.html.twig') }}
{% endblock %}
