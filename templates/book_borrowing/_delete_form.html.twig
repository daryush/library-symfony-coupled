<form method="post" action="{{ path('book_borrowing_delete', {'id': book_borrowing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book_borrowing.id) }}">
    <button class="btn">Delete</button>
</form>
